# ULS Frontend

Run locally:

1. Set `NEXT_PUBLIC_API_BASE` to your backend URL, e.g. `http://localhost:4000`.
2. Install: `npm install`
3. Start: `npm run dev`

Login with `admin/admin` or `dev/dev` to get UI token.
